# 印刷跟单工作台 — 部署说明

## 一、需要什么环境

找一台公司内网的电脑（或者云服务器），装好 Python 3 就行。

### 检查 Python 是否已安装

打开终端（Mac）或命令提示符（Windows），输入：

```
python3 --version
```

如果显示 `Python 3.x.x` 就OK。如果没有，先去 https://www.python.org/downloads/ 下载安装。

---

## 二、部署步骤（3 步搞定）

### 第 1 步：把文件放到服务器上

将 `print-tracker` 整个文件夹拷贝到服务器任意目录，比如：

```
/home/你的用户名/print-tracker/
```

或者 Windows 上：

```
C:\print-tracker\
```

文件夹里应该有这三个文件：
- `server.py` — 后端服务
- `index.html` — 前端页面
- `orders.db` — 数据库文件（里面有演示数据，部署后可以删掉重新开始）

### 第 2 步：安装依赖

打开终端，进入项目目录：

```bash
cd /home/你的用户名/print-tracker/
```

安装 Flask（只需要这一个依赖）：

```bash
pip3 install flask
```

### 第 3 步：启动服务

```bash
python3 server.py
```

看到以下输出说明启动成功：

```
📋 印刷跟单工作台启动！
   数据库: /home/xxx/print-tracker/orders.db
   访问: http://0.0.0.0:5000
```

---

## 三、访问工作台

### 本机访问
浏览器打开：`http://localhost:5000`

### 局域网其他电脑/手机访问
先查看服务器 IP 地址：

**Mac/Linux：**
```bash
ifconfig | grep "inet "
```

**Windows：**
```bash
ipconfig
```

找到类似 `192.168.1.xxx` 的地址，然后在其他设备的浏览器打开：

```
http://192.168.1.xxx:5000
```

### 固定网址
建议把服务器的 IP 设为固定 IP（在路由器里设置 DHCP 静态分配），这样网址永远不变。

---

## 四、长期运行（让服务一直跑）

上面的 `python3 server.py` 关掉终端就停了。需要后台持续运行：

### Mac/Linux 推荐方式：使用 nohup

```bash
cd /home/你的用户名/print-tracker/
nohup python3 server.py > server.log 2>&1 &
```

这样关掉终端也不会停。

### 开机自启（Mac）

编辑 `~/Library/LaunchAgents/com.print.tracker.plist`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.print.tracker</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/你的用户名/print-tracker/server.py</string>
    </array>
    <key>WorkingDirectory</key>
    <string>/Users/你的用户名/print-tracker</string>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
</dict>
</plist>
```

然后执行：

```bash
launchctl load ~/Library/LaunchAgents/com.print.tracker.plist
```

### 开机自启（Windows）

创建一个 `start.bat` 文件放在 `C:\print-tracker\`：

```bat
@echo off
cd /d C:\print-tracker
python server.py
```

然后把这个 bat 文件的快捷方式放到 Windows 启动文件夹：
`Win+R` → 输入 `shell:startup` → 粘贴快捷方式。

---

## 五、数据备份

数据库文件就是 `orders.db`，定期复制一份就行：

```bash
cp orders.db orders_backup_$(date +%Y%m%d).db
```

建议每周备份一次，存到云盘或 U 盘。

---

## 六、常见问题

**Q: 手机打不开？**
A: 确保手机和服务器在同一个 WiFi/局域网下。

**Q: 想从外网访问？**
A: 需要在路由器做端口映射（把 5000 端口映射到服务器 IP），然后通过公网 IP 访问。建议找技术同事帮忙配置。

**Q: 想清空演示数据重新开始？**
A: 删掉 `orders.db` 文件，重启服务会自动创建空数据库。

**Q: 数据越来越多，会不会变慢？**
A: SQLite 在几万条数据内都没问题，印刷跟单用几年也到不了这个量。

---

## 七、文件清单

| 文件 | 说明 |
|------|------|
| `server.py` | 后端服务，不要改 |
| `index.html` | 前端页面，不要改 |
| `orders.db` | 数据库文件，**定期备份这个** |
| `部署说明.md` | 本文件 |
